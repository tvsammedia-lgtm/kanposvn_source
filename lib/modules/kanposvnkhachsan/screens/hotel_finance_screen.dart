import 'package:flutter/material.dart';

class HotelFinanceScreen extends StatelessWidget {
  const HotelFinanceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Kế toán & Thu chi'));
  }
}
