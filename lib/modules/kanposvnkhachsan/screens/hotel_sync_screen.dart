import 'package:flutter/material.dart';

class HotelSyncScreen extends StatelessWidget {
  const HotelSyncScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Đồng bộ Vercel Neon'));
  }
}
