import 'package:flutter/material.dart';

class VantaiRoutesVehiclesScreen extends StatelessWidget {
  const VantaiRoutesVehiclesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Tính năng Quản lý Tuyến và Xe (Đang phát triển)'),
      ),
    );
  }
}
